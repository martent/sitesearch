analysis:
  analyzer:
    term_index:
      tokenizer: edge_ngram_2
      filter:
      - lowercase
      - asciifolding
    term_search:
      tokenizer: keyword
      filter:
      - asciifolding
      - lowercase
  tokenizer:
    edge_ngram_2:
      type: edgeNGram
      min_gram: 2
      max_gram: 50
      token_chars:
      - letter
      - digit
      - whitespace
