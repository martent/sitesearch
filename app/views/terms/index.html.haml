- title "Sökord"
%section.box.index
  %h1.box-title= h1
  .box-content.body-copy
    .table-wrapper
    %table.full
      %thead
        %tr
          %th Sökord
          %th.auth Rekommendation
      %tbody
        - @terms.each do |term|
          %tr
            %td= term.name
            %td= link_to term.recommendation.name, edit_recommendation_path(term.recommendation)
